accessWidener	v2	official

# NOTE: an entry for Entity#unsetRemoved used to live here, for a discard/re-add
# re-spawn hack that turned out to leave a ghost copy of the bot on the client.
# That hack is gone (see PracticeBotManager#performSummon), so the access is unused.

# NOTE: entries for Entity#setSharedFlag and LivingEntity#useItemRemaining used to live
# here. Both existed only to hand-maintain per-tick state that FakePlayer#tick() never
# ran (clearing the stuck fall-flying flag; counting down the shield's block delay).
# FakePlayerTickMixin now runs the real player tick instead -- Carpet's approach -- so
# vanilla maintains both itself and the widened access is no longer needed.
